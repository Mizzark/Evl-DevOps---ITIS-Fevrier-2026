---
name: Deploiement NGINX

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on:
      - self-hosted
      - linux
      - x64
    steps:
      - name: Démarrage 2 NGINX
        run: sudo systemctl start nginx
